# This file contains names only. Service-specific values will be introduced
# together with the module that owns them.
PROJECT_NAME=power-grid-telemetry-monitor
APP_ENV=development

# Milestone 1 — Simulator
SIMULATOR_INTERVAL_SECONDS=5
SIMULATOR_SEED=42
